#include<iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    int k;
    cin >> n >> k;
    vector<string> cards(n);
    for(int i=0; i<n; i++){
        cin >> cards[i];
    }

    

    /*
    int comb = pow(n,k);
    vector<vector<int>> comb_num（comb, vector<int> (n));
    int count_k = k;
    while(count_k>=0){
        countk -= 1;
        for(int i=0; i<n; i++){
            
        }
    }
    */
}